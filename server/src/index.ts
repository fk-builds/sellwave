import express from 'express'; import helmet from 'helmet'; import cors from 'cors'; import cookieParser from 'cookie-parser'; import { env } from './config/env.js'; import auth from './routes/auth.js'; import catalog from './routes/catalog.js'; import admin from './routes/admin.js'; import cart from './routes/cart.js'; import account from './routes/account.js'; import wishlist from './routes/wishlist.js'; import returns from './routes/returns.js'; import shipping from './routes/shipping.js'; import reviews from './routes/reviews.js'; import orders from './routes/orders.js'; import settings from './routes/settings.js'; import opsAdmin from './routes/ops-admin.js'; import pricingAgent from './routes/pricing-agent.js'; import { errorHandler } from './middleware/errors.js'; import { prisma } from './utils/prisma.js';
const app=express(); app.set('trust proxy',1);app.use(helmet());app.use(cors({origin:env.CLIENT_ORIGIN,credentials:true}));app.use(express.json({limit:'1mb'}));app.use(cookieParser());app.get('/api/health',(_q,res)=>res.json({status:'ok'}));
app.get('/api/sitemap.xml',async(_q,res)=>{const base='https://sellwave.pk';const[products,categories]=await Promise.all([prisma.product.findMany({where:{status:'ACTIVE'},select:{slug:true,updatedAt:true}}),prisma.category.findMany({where:{isActive:true},select:{slug:true}})]);const urls=['/', '/shop','/about','/support','/returns',...categories.map(c=>`/shop?category=${c.slug}`),...products.map(p=>`/product/${p.slug}`)];res.set('Content-Type','application/xml');res.send(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(u=>`<url><loc>${base}${u}</loc></url>`).join('')}</urlset>`);});app.use('/api/auth',auth);app.use('/api',catalog);app.use('/api/admin/ops',opsAdmin);app.use('/api/admin/pricing-agent',pricingAgent);app.use('/api/admin',admin);app.use('/api/cart',cart);app.use('/api/account',account);app.use('/api/wishlist',wishlist);app.use('/api/returns',returns);app.use('/api/admin/shipping',shipping);app.use('/api/reviews',reviews);app.use('/api/orders',orders);app.use('/api/settings',settings);app.use(errorHandler);if (!process.env.VERCEL) { app.listen(env.PORT, '0.0.0.0', () => console.log(`Sell Wave API listening on ${env.PORT}`)); }
export default app;
