{
  "name": "sellwave-single-store",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run db:generate -w server && npm run build -w server && npm run build -w web",
    "db:generate": "npm run db:generate -w server",
    "db:migrate": "npm run db:migrate -w server",
    "seed": "npm run seed -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}