import { OrderDetail } from './OrderDetail';

/** After checkout the customer lands here — same full order view with tracking timeline. */
export function OrderConfirmation() {
  return <OrderDetail />;
}
